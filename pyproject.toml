[project]
name = "threejs-mcp"
version = "0.2.0"
description = "Three.js code-generation MCP server"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.26.0",
]

[project.scripts]
threejs-mcp = "server:mcp.run"
