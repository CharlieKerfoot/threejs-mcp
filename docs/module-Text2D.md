# Text2D

## Import

## Methods
- `.createText( message :string, height :number) :Mesh` — A helper function for creating a simple plane mesh
that can be used as a text label. The mesh's material
holds a canvas texture that displays the given message.

## Source