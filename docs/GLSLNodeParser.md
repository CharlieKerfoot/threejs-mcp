# GLSLNodeParser
Extends: NodeParser→

A GLSL node parser.

## Constructor
`newGLSLNodeParser()`

## Methods
- `.parseFunction( source :string) :GLSLNodeFunction` — The method parses the given GLSL code an returns a node function.

## Source