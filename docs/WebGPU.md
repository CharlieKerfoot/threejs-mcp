# WebGPU

A utility module with basic WebGPU capability testing.

## Import

## Static Methods
- `.getErrorMessage() : HTMLDivElement` — Returns a div element representing a formatted error message that can be appended in
web sites if WebGPU isn't supported.
- `.isAvailable() : boolean` — Returns true if WebGPU is available.

## Source