# InstancedBufferGeometry

An instanced version of a geometry.

## Constructor
`newInstancedBufferGeometry()`
Constructs a new instanced buffer geometry.

## Properties
- `.instanceCount : number` — The instance count. Default is Infinity .
- `.isInstancedBufferGeometry : boolean` — This flag can be used for type testing. Default is true .

## Source