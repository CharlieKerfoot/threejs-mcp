# LightingNode
Extends: EventDispatcher→Node→

Base class for lighting nodes.

## Constructor
`newLightingNode()`
Constructs a new lighting node.

## Properties
- `.isLightingNode : boolean` — This flag can be used for type testing. Default is true .

## Source