# Int8BufferAttribute
Extends: BufferAttribute→

Convenient class that can be used when creating a Int8 buffer attribute with
a plain Array instance.

## Constructor
`newInt8BufferAttribute( array :Array.<number> | Int8Array, itemSize :number, normalized :boolean)`
Constructs a new buffer attribute.

## Source