# SVGObject
Extends: EventDispatcher→Object3D→

Can be used to wrap SVG elements into a 3D object.

## Constructor
`newSVGObject( node :SVGElement)`
Constructs a new SVG object.

## Import

## Properties
- `.isSVGObject : boolean` — This flag can be used for type testing. Default is true .
- `.node : SVGElement` — This SVG element.

## Source