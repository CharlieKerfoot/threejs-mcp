# GeometryCompressionUtils

## Import

## Methods
- `.compressNormals( geometry :BufferGeometry, encodeMethod :'DEFAULT' | 'OCT1Byte' | 'OCT2Byte' | 'ANGLES') (inner)` — Compressed the given geometry's normal attribute by the selected encode method.
- `.compressPositions( geometry :BufferGeometry) (inner)` — Compressed the given geometry's position attribute.
- `.compressUvs( geometry :BufferGeometry) (inner)` — Compressed the given geometry's uv attribute.

## Source