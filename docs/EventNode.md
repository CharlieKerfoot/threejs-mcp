# EventNode
Extends: EventDispatcher→Node→

EventNode is a node that executes a callback during specific update phases.

## Constructor
`newEventNode( eventType :string, callback :function)`
Creates an EventNode.

## Source