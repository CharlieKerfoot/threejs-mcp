# LUTPass
Extends: Pass→ShaderPass→

Pass for color grading via lookup tables.

## Constructor
`newLUTPass( options :Object)`
Constructs a LUT pass.

## Import

## Properties
- `.intensity : number` — The intensity. Default is 1 .
- `.lut :Data3DTexture` — The LUT as a 3D texture. Default is null .

## Source