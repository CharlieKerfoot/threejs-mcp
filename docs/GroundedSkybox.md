# GroundedSkybox
Extends: EventDispatcher→Object3D→Mesh→

A ground-projected skybox. By default the object is centered at the camera, so it is often helpful to set skybox.position.y = height to put the ground at the origin.

## Constructor
`newGroundedSkybox( map :Texture, height :number, radius :number, resolution :number)`
Constructs a new ground-projected skybox.

## Import

## Source