# InstancedMeshNode
Extends: EventDispatcher→Node→InstanceNode→

This is a special version of InstanceNode which requires the usage of InstancedMesh .
It allows an easier setup of the instance node.

## Constructor
`newInstancedMeshNode( instancedMesh :InstancedMesh)`
Constructs a new instanced mesh node.

## Properties
- `.instancedMesh :InstancedMesh` — A reference to the instanced mesh.

## Source