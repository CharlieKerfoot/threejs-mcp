# DDSLoader
Extends: Loader→CompressedTextureLoader→

A loader for the S3TC texture compression format.

## Constructor
`newDDSLoader( manager :LoadingManager)`
Constructs a new DDS loader.

## Import

## Methods
- `.parse( buffer :ArrayBuffer, loadMipmaps :boolean) :CompressedTextureLoader~TexData` — Parses the given S3TC texture data.

## Source