# AudioContext

Manages the global audio context in the engine.

## Static Methods
- `.getContext() :AudioContext` — Returns the global native audio context.
- `.setContext( value :AudioContext)` — Allows to set the global native audio context from outside.

## Source